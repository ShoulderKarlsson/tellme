{
  "name": "info-cli",
  "version": "1.0.0",
  "description": "simple help cli",
  "main": "index.js",
  "author": "shoulderkarlsson",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "backpack",
    "start": "node build/main.js",
    "build": "backpack build",
    "dev-week": "yarn build && node build/main.js --week",
    "dev-network": "yarn build && node build/main.js -n",
    "dev-leap": "yarn build && node build/main.js --leap-year",
    "dev-red": "yarn build && node build/main.js -r"
  },
  "dependencies": {
    "colors": "^1.2.4",
    "commander": "^2.15.1",
    "date-fns": "^1.29.0",
    "isomorphic-fetch": "^2.2.1"
  },
  "devDependencies": {
    "backpack-core": "^0.7.0"
  }
}
